// GSH instance ships a complete pt-BR catalog, so new accounts should start there instead of SOURCE_LOCALE ('en', the i18n extraction source language).
export const DEFAULT_NEW_USER_LOCALE = 'pt-BR';
